# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / Vite
web/node_modules/
web/dist/

# Generated site output — rebuilt from data/obituaries_master.json every run by
# extract/main.py (`--render-only` regenerates locally). The master itself IS
# committed (it is the source of truth); these derivatives are not.
web/public/data/obituaries.json
web/public/sitemap.xml
web/public/feed.xml
web/public/robots.txt
web/public/o/*.html
!web/public/o/.gitkeep
web/public/funeral-home/
web/public/assets/og/
data/failures.json

# Build caches (not committed; CI restores these via actions/cache)
.cache/

# Local env
.env

# Claude Code local settings
.claude/settings.local.json
