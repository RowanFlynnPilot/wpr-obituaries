---
target: the register widget (web/src/App.jsx)
total_score: 30
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
target_identity: "file:C:\\Users\\rpfly\\Projects\\wpr-obituaries\\web\\src\\App.jsx"
target_fingerprint: "sha256:466f832882f21942228dce1c970e0cedc49d9adcf2b5eeeec63b67bd3b8967f1"
target_path: "C:\\Users\\rpfly\\Projects\\wpr-obituaries\\web\\src\\App.jsx"
timestamp: 2026-09-12T02-50-37Z
slug: web-src-app-jsx
---
Method: dual-agent (A: ae7762537278a6521 · B: a605dbe6cf1cc5bd6)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Count line always scoped, single live region; mention rows give no visible reason they matched |
| 2 | Match System / Real World | 4 | "Died …"/"Published …", "Find a name", lifespan · town; "3 more mentions" is the only unnatural phrase |
| 3 | User Control and Freedom | 3 | Every state has a URL; "Clear the search" drops focus to body; opening Browse removes the carousel |
| 4 | Consistency and Standards | 3 | Pause reads "Play, pressed"; flag opens _blank, rows open _top |
| 5 | Error Prevention | 3 | Prefix + diacritic folding; "Jenson" is a hard zero |
| 6 | Recognition Rather Than Recall | 3 | Only-existing letters and chip counts; mentions force recall of why a row is there |
| 7 | Flexibility and Efficiency | 3 | Roving-tabindex dots, Enter dismisses keyboard, deep links work |
| 8 | Aesthetic and Minimalist Design | 3 | 286px of carousel before the first desktop name; sponsors in two placements |
| 9 | Error Recovery | 3 | Empty state excellent; fetch-error state has no retry or phone |
| 10 | Help and Documentation | 2 | Nothing explains "mentions"; placeholder clips at 375px |
| **Total** | | **30/40** | **Good** |

Applicable maximum 40; no heuristics n/a. Previous runs: 26, 31. The drop from 31 is reviewer calibration (7 rose 2→3 on URL state; 1 and 9 each lost a point on stricter judgment of the mentions tier and the error state); all round-2 fixes verified.

## Design Specificity Verdict

**LLM assessment:** Authored for this newsroom on structural evidence: the flag closed by the 3-over-1 ink rule with the title below, "In Memoriam" oxblood Courier kicker, date-of-death headings, typewriter fact line, funeral-home caption per row, colophon ending on the phone number. No off-palette pixel found, including the redrawn clear glyph. Slips generic only in the desktop carousel (card/arrows/dots/Pause), which sits between the search and its results, and in the lede sentence.

**Deterministic scan:** `impeccable detect --json web/src` → exit 0, zero findings. In-page detector: 32 — 28 `all-caps-body` (27 on `.entry__home` index.css:869-876, 1 on `.masthead__tagline` 115-122), 1 `hero-eyebrow-chip` (`.masthead__eyebrow` 132-139), 2 `line-length` on `.footer__lines p` (measured ~60 real chars/line under the 72ch cap), 1 `cream-palette` on body (`--paper #f6f2ea`), 1 `dark-glow` = the detector's own overlay color `#ffba00` (not in source). All 32 false positives against the brief.

**Visual overlays:** injection succeeded ([Human] tab), overlays drawn, live server on 8400 stopped, tab closed. Measurements: no overflow at 1024 or 375; search top 466 desktop / 449 mobile; first row 880–1002 desktop / 667 mobile; 12/12 tab stops with the oxblood focus-visible ring at both widths; 0 targets under 24px (41 of 105 under 44px; letters 24×24 smallest); no `transition: all`; every muted label 4.81:1; `?q=jensen` written on typing; 2 live regions at steady state.

## Overall Impression

The peak works: "Allan Jensen" collapses the page to one heading, one portrait, three facts, one sentence. Between the searcher and that moment sits the desktop carousel; after it, the mentions tier undermines trust — rows with no evidence, and "Schmidt" producing 69 of them because it is also a funeral home's name. Biggest opportunity: make a mention explain itself or stop being a row.

## What's Working

1. The fact line as confirmation outside the sentence, beneath a "Died …" heading.
2. State hygiene: every state writes a clean URL and restores exactly; Back still leaves the page.
3. The carousel keyboard model: dots as one stop, arrows move the card, live region reads the change, auto-advance stops.

## Priority Issues

**[P1] Mentions tier shows no evidence and collides with funeral-home names**
- Why: "jensen" lists Donna Rogers with no visible Jensen; "Schmidt" → 4 names + 69 mentions (Schmidt & Schulta). A row with no reason reads as a wrong answer on a memorial.
- Fix: home-name match → one line "69 notices arranged by Schmidt & Schulta Funeral Home →" setting the home filter; summary mentions → `.entry__match` line (Courier 11.5px, hit in `<mark>`), cap 10 with "N more"; heading "Also named in N other notices".
- Command: /impeccable clarify

**[P1] Desktop: first name at 1002px with the carousel between field and results**
- Why: DESIGN.md's rule is "nothing between the search and its results"; sponsor line is already in the nameplate.
- Fix: render the carousel after the first register group, or shrink to a 66×66 five-portrait strip, names only, ≤120px.
- Command: /impeccable layout

**[P2] Carousel live region announces every 6.5s while auto-advancing**
- Fix: render "Now showing …" only after a user-initiated change (`stopped` or focus within).
- Command: /impeccable harden

**[P2] Browse unmounts the carousel (320px jump + parent-page scroll in the iframe); Clear drops focus to body**
- Fix: drop `!browseOpen` from `isDefault`, remove the scrollIntoView; focus `#obit-search` after Clear and the first `.browse__letter` after "Browse by last name".
- Command: /impeccable harden

**[P2] Pause is a double-state control ("Play, pressed")**
- Fix: keep the label swap, remove `aria-pressed`, add aria-label "Pause auto-advance"/"Resume auto-advance".
- Command: /impeccable polish

**[P3] Phone details**
- Placeholder clips at 375px → "Name, town or funeral home"; letters 32px and chips 36px under the phone query; toggle copy "Browse by month, name, town or home".
- Command: /impeccable adapt

## Persona Red Flags

**Jordan:** first desktop name at 1002px; confirming facts in 11–12px Courier; "3 more mentions" meaningless; clipped placeholder.
**Sam:** live-region chatter every 6.5s; "Play, pressed"; two sponsor links before the field; no heading for the carousel.
**Casey:** search thumb-reachable at 449–502px; "Show earlier" at 13,363px; open Browse 438px; 24px letters.
**Half-remembering searcher:** "Jenson" → 0; "Tugnoli" fails on "Latzig-Tugnoli" (hyphen); "Schmidt" drowns in mentions; hint appears only after the dead end.

## Minor Observations

- `.browse__toggle` declares `text-align: left` twice.
- `role="status"` + `aria-live="polite"` on the count line is redundant.
- Flag opens a new tab while rows replace the parent page.
- Fetch-error branch: no retry, no colophon phone.
- "No obituaries to show here yet." is unreachable in practice.
- The carousel live region stays in the DOM on phones while `display:none`.

## Questions to Consider

- Who is the desktop carousel for? Would anyone miss it after the first day's names?
- Is a strict prefix match honest enough to say "No one named X is listed yet", or does the register owe one fuzzy pass first?
- Should a mention ever be a row, or a single expandable sentence?
